<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <global-method-security  secured-annotations="enabled" pre-post-annotations="enabled"/> 
   
	<!-- HTTP security configurations -->
	<http access-denied-page="/views/dataAccessFailure.html" auto-config="true" use-expressions="true">
		<form-login login-processing-url="/resources/j_spring_security_check"
			login-page="/" default-target-url="/patients/home.html"
			authentication-failure-url="/index.html?login_error=1" /> 
		<logout logout-url="/resources/j_spring_security_logout" logout-success-url="/" />
		<!-- Configure these elements to secure URIs in your application -->
		<intercept-url pattern="/choices/**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/patients/**" access="isAuthenticated()" />
		<intercept-url pattern="/clinicaldocuments/**" access="isAuthenticated()" />
		<intercept-url pattern="/consents/**" access="isAuthenticated()" />
		<intercept-url pattern="/**" access="permitAll" />
		<logout invalidate-session="true" logout-url="/index.html" delete-cookies="true" />
		<!-- <intercept-url pattern= "/resources/**" security="none"/> -->
		
		
		<session-management session-fixation-protection="migrateSession" invalid-session-url="/index.html" >
			<concurrency-control max-sessions="5" error-if-maximum-exceeded="false" expired-url="/index.html"  />
                      
         </session-management>  
		
		
	</http>

	<!-- Configure Authentication mechanism -->
	<authentication-manager alias="authenticationManager">
		<authentication-provider>
			<password-encoder ref="passwordEncoder"/>
			<jdbc-user-service id="userDetailsService"
				data-source-ref="dataSource" />
		</authentication-provider>
	</authentication-manager>

	<!-- Uses SHA-256 with multiple iterations and a random salt value. -->
	<beans:bean id="passwordEncoder"
		class="org.springframework.security.crypto.password.StandardPasswordEncoder" />
</beans:beans>
